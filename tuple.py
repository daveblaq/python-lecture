# Tupples are used to store multiple items in a single variable
three_numbers = (1, 2, 3)
print(three_numbers[0])

# Create a list with the tuple construct
three_numbers = tuple(three_numbers)
print(three_numbers)